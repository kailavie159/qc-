## Coincu Delete Batch Summary - 2026-05-15

- `delete_now`: `68` URLs
- `delete_leaning`: `213` URLs

### Recommended Order

1. Run `delete_now` first on date-bound, stale, zero-signal posts.
2. Review `delete_leaning` next; delete when the topic is clearly low-value and no strategic coverage is needed.
3. Leave evergreen-like guides, explainers, and wallet/how-to topics out of bulk delete unless manually confirmed.

### Notes

- These batches are content-side only and do not solve the legacy media root cause by themselves.
- After delete actions, recheck whether broken-image discovery shrinks on affected URLs.
